import React from 'react';
import { Col, Button, Form, FormGroup, Label, Input, FormText } from 'reactstrap';
import './index.css';

/**
 * A component that returns the layout of the home page
 */
export const Home = () => {
    return (<div>Hom,e</div>);
}